// -------------------------------------
// LookBook: JS > Paroller Initialize
// -------------------------------------

// Initializes paroller.js and sets attributes for selected elements

$(".gallery__gallery-item:nth-child(12n+1), [data-paroller-factor]").paroller({
	factor: 0.05,            	// multiplier for scrolling speed and offset
	type: 'foreground',     	// background, foreground
	direction: 'vertical' 		// vertical, horizontal
});

$(".gallery__gallery-item:nth-child(12n+2), [data-paroller-factor]").paroller({
	factor: 0.0,            	// multiplier for scrolling speed and offset
	type: 'foreground',     	// background, foreground
	direction: 'vertical' 		// vertical, horizontal
});

$(".gallery__gallery-item:nth-child(12n+3), [data-paroller-factor]").paroller({
	factor: 0.2,            	// multiplier for scrolling speed and offset
	type: 'foreground',     	// background, foreground
	direction: 'vertical' 		// vertical, horizontal
});

$(".gallery__gallery-item:nth-child(12n+4), [data-paroller-factor]").paroller({
	factor: 0.1,            	// multiplier for scrolling speed and offset
	type: 'foreground',     	// background, foreground
	direction: 'vertical' 		// vertical, horizontal
});

$(".gallery__gallery-item:nth-child(12n+5), [data-paroller-factor]").paroller({
	factor: 0.05,            	// multiplier for scrolling speed and offset
	type: 'foreground',     	// background, foreground
	direction: 'vertical' 		// vertical, horizontal
});

$(".gallery__gallery-item:nth-child(12n+6), [data-paroller-factor]").paroller({
	factor: 0.15,            	// multiplier for scrolling speed and offset
	type: 'foreground',     	// background, foreground
	direction: 'vertical' 		// vertical, horizontal
});

$(".gallery__gallery-item:nth-child(12n+7), [data-paroller-factor]").paroller({
	factor: 0.05,            	// multiplier for scrolling speed and offset
	type: 'foreground',     	// background, foreground
	direction: 'vertical' 		// vertical, horizontal
});

$(".gallery__gallery-item:nth-child(12n+8), [data-paroller-factor]").paroller({
	factor: 0.1,            	// multiplier for scrolling speed and offset
	type: 'foreground',     	// background, foreground
	direction: 'vertical' 		// vertical, horizontal
});

$(".gallery__gallery-item:nth-child(12n+9), [data-paroller-factor]").paroller({
	factor: 0.05,            	// multiplier for scrolling speed and offset
	type: 'foreground',     	// background, foreground
	direction: 'vertical' 		// vertical, horizontal
});

$(".gallery__gallery-item:nth-child(12n+10), [data-paroller-factor]").paroller({
	factor: 0.15,            	// multiplier for scrolling speed and offset
	type: 'foreground',     	// background, foreground
	direction: 'vertical' 		// vertical, horizontal
});

$(".gallery__gallery-item:nth-child(12n+11), [data-paroller-factor]").paroller({
	factor: 0.2,            	// multiplier for scrolling speed and offset
	type: 'foreground',     	// background, foreground
	direction: 'vertical' 		// vertical, horizontal
});

$(".gallery__gallery-item:nth-child(12n), [data-paroller-factor]").paroller({
	factor: 0.1,            	// multiplier for scrolling speed and offset
	type: 'foreground',     	// background, foreground
	direction: 'vertical' 		// vertical, horizontal
});